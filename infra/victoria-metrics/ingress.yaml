apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: victoria-metrics
  namespace: observability
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`metrics.homelab.internal`)
      kind: Rule
      services:
        - name: victoria-metrics
          port: 8428
