apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: phoenix
  namespace: observability
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`phoenix.homelab.internal`)
      kind: Rule
      services:
        - name: phoenix
          port: 6006
