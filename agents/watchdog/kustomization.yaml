apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - cronjob.yaml
configMapGenerator:
  - name: watchdog-rules
    files:
      - rules.yaml
generatorOptions:
  disableNameSuffixHash: true
